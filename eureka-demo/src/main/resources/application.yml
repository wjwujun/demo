server:
  port: 10087
spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: true   #把自己做为eureka服务注册
    fetch-registry: true      #拉取服务列表
    service-url:    #eureka 服务注册地址
      defaultZone: http://127.0.0.1:10086/eureka


